---
title: Core Activity Core A
blocks:
  - body: |
      # CORE A
    _template: content
  - body: >
      ```javascript

      ```


      ```mermaid

      %% This won't render without implementing a rendering engine (e.g. mermaid
      on npm)

      flowchart TD
          id1(this is an example flow diagram) 
          --> id2(modify me to see changes!)
          id2 
          --> id3(Click the top button to preview the changes)
          --> id4(Learn about mermaid diagrams - mermaid.js.org)
      ```


      ```javascript

      ```


      <Image
        image="/uploads/CoreA.jpg"
        width={300}
        height={4000}
        float="left"
        caption={<>
          IMAGE
          
          Here is an image of Core A. It is a long core section, so scroll down. The clear round pins on the left indicate the depth from the top of the core marked every 10 cm (ex: 800 = 800 cm down from the sediment surface).
        </>}
        showCaption={false}
      />
    _template: content
---

